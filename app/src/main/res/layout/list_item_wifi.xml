<?xml version="1.0" encoding="utf-8"?>
<!-- This is a simplified, more compatible layout for a single list item. -->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingHorizontal="8dp"
    android:paddingVertical="4dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/surface_dark"
        android:padding="16dp">

        <ImageView
            android:id="@+id/wifi_icon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            tools:src="@drawable/ic_wifi_strength_4" />

        <ImageView
            android:id="@+id/security_icon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            tools:src="@drawable/ic_lock" />

        <TextView
            android:id="@+id/signal_strength_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:layout_toStartOf="@id/security_icon"
            android:layout_centerVertical="true"
            android:textColor="@color/text_secondary_dark"
            android:textSize="16sp"
            tools:text="-55 dBm" />

        <TextView
            android:id="@+id/network_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_toEndOf="@id/wifi_icon"
            android:layout_toStartOf="@id/signal_strength_text"
            android:layout_centerVertical="true"
            android:textColor="@color/text_primary_dark"
            android:textSize="18sp"
            android:textStyle="bold"
            tools:text="My Home Network" />

    </RelativeLayout>
</FrameLayout>
